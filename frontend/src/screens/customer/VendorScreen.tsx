import React from 'react'
import { Text, View } from 'react-native'

export const VendorScreen = () => {
  // show those vendor to the customer who have added them as a customer 
  // and when cliked on the vendor show their products in a new screen
  return (
    <Text>MyProductsScreen</Text>
  )
}
